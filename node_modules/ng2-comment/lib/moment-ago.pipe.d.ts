/// <reference types="core-js" />
import { PipeTransform } from '@angular/core';
export declare class MomentAgoPipe implements PipeTransform {
    transform(value: Date): string;
    private stringify(timeDiff, divisor, unit);
}

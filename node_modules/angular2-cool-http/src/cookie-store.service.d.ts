export declare class CookieStore {
    lastReadRawCookieCollection: string;
    lastReadCookieCollection: any;
    getCookie(key: string): string;
    getCookieCollection(): any;
    private static tryDecodeUriComponent(uriComponent);
}
